{
  "name": "gaffer",
  "private": true,
  "description": "Youth soccer team/game-day management app — dependency-free vanilla JS, localStorage only. This package.json exists only for the Playwright regression suite in tests/.",
  "scripts": {
    "test": "node tests/run.js"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
